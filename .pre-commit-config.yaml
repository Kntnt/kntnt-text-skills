repos:
  - repo: local
    hooks:
      - id: audit
        name: kntnt-text-skills audit
        entry: python scripts/audit.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
