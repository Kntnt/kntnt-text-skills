repos:
  - repo: local
    hooks:
      - id: audit
        name: kntnt-text-skills audit
        entry: uv run scripts/audit.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
